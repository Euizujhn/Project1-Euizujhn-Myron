Team member: Euizujhn wade, Myron Rose
Peer review group: C3 – Kirby, Camden / Shopher, Jacob


-One good thing about the code is that The logic is broken down into readable steps: parsing, redirect handling, checking page validity, checking revisions, and then printing.

-The code uses early returns to avoid deep nesting, which improves readability.

-The class FormatJsonData is responsible for both parsing and output (i.e., formatting + printing), violating the Single Responsibility Principle (SRP)

-Break into smaller private methods:

-Refactor to return data instead of printing directly.

